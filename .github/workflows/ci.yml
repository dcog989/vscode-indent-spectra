name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup dependencies
        run: |
          # TODO: Add language-specific setup
          echo "TODO: Configure CI for web"
      - name: Run tests
        run: echo "TODO: Add test command"
